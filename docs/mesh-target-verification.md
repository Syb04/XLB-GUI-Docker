# 総セル数指定の検証（2026-09-17）

- WSLで既存・HTTPを含む74件の全テストが成功。追加の目標セル数テスト10件とHTTPテストの再確認も成功（重複を除いて84件）。
- 3:1:1のボックスに300万セルを指定し、300×100×100の実メッシュを生成。等方セル幅と元の寸法を確認。
- 目標3500に対して33×11×11＝3993セル。見積もり・保存後の再読込・メッシュ生成の一致を確認。
- 非整数寸法のCADをインポートし、古いgeometry.sizeに依存せず実際のCAD境界から分割。等方性・外周余白・見積もりと生成の一致を確認。
- 障害物の各種計算領域記法、実際の外側寸法に基づく緩和時間検査、上限、極端に薄いCAD、分割不能な寸法比、旧メッシュ指定の互換性を確認。
- 通常Docker環境のGUIで総数の編集と3993セルの生成を確認。CPU・CUDA両イメージをビルドし、CUDA版を8766に反映。
- 総数指定から実CUDA計算を2ステップ実行して正常終了。run `c2ccb0e4308546ff9d046268073808c8`、project `0413f489a66a48b8bd43d0b9477963a7`。形状33×11×11、device `cuda:0`、compute_backend `device`、XLB D3Q27 BGK。

これは入力・格子生成・既存GPU経路の結合検証です。新たな物理精度検証を意味しません。
